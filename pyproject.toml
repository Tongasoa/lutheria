[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
markers = [
    "integration: tests utilisant les vrais modèles ML (lents, désactivés par défaut)",
]
addopts = "-m 'not integration'"
